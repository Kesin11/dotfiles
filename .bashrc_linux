# alias
if [ -f ~/.bash_aliases ]; then
    . ~/.bash_aliases
fi

# git completion:
# debian
if [ -f /usr/lib/git-core/git-sh-prompt ]; then
    source /usr/lib/git-core/git-sh-prompt
    PS1="\u@\h:\W\$(__git_ps1)\$ "
fi
export PS1
